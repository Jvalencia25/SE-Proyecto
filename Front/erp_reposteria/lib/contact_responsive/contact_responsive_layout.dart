import 'package:flutter/material.dart';

class ContactResponsiveLayout extends StatelessWidget {
  const ContactResponsiveLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}